// 闭包计数器测试：利用闭包实现私有状态的计数器
// EXPECT: 6
fn createCounter() {
    count = 0
    fn inc() {
        count = count + 1
        return count
    }
    return inc
}
c = createCounter()
result = c() + c() + c()
