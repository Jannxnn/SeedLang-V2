// 编译器优化：for-in 循环体内变量被重新赋值时使用 let 而非 const
// 验证 sum 在 for-in 体内先赋 0 再在 for 循环中累加，必须用 let
// EXPECT: 45
total = 0
inputs = [10]
for n in inputs {
  sum = 0
  for (i = 0; i < n; i = i + 1) {
    sum = sum + i
  }
  total = total + sum
}
result = total
