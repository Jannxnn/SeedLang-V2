// 编译器优化：for-in 循环体含数组操作时提取为独立函数
// 验证数组创建、填充、求和在 for-in 提取后仍正确执行
// EXPECT: 4
total = 0
inputs = [3 2]
for n in inputs {
  arr = []
  for (i = 0; i < n; i = i + 1) {
    push(arr i)
  }
  s = 0
  for (i = 0; i < n; i = i + 1) {
    s = s + arr[i]
  }
  total = total + s
}
result = total
