// 场景测试 - retry once：单次重试机制
// EXPECT: "ok"
count = 0
fn op() {
    count = count + 1
    if count < 2 { return null }
    return "ok"
}
fn retry(n) {
    i = 0
    while i < n {
        r = op()
        if r != null { return r }
        i = i + 1
    }
    return null
}
result = retry(3)
